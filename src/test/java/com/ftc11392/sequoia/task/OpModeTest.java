package com.ftc11392.sequoia.task;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class OpModeTest {
    @DisplayName("Test Opmode Lifecycle")
    @Test
    public void testOpmodeLifecycle() {
        // TODO create test that goes over every loop of SequoiaOpMode, and ensures that each loop is run a set number of times
        // This also means that it must ensure that loops that run once only run once
    }

    @DisplayName("Test Opmode Repeatability")
    @Test
    public void testOpmodeRepeatability() {
        // TODO create test that ensures that the OpMode behaves exactly the same when run multiple times
    }

    @DisplayName("Test Opmode Stop")
    @Test
    public void testOpmodeStop() {
        //TODO create test that ensures OpMode stops within a reasonable amount of time when stop() is called
    }
}
